import 'package:flutter/material.dart';

enum Ps5ControllerColors {
  blueGrey(Colors.blueGrey),
  red(Colors.red),
  amber(Colors.amber),
  blue(Colors.blue),
  green(Colors.green);

  final Color color;

  const Ps5ControllerColors(this.color);
}

